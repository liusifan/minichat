/* addrbook_tool_main.cpp

 Generated by phxrpc_pb2tool from addrbook.proto

*/

#include <time.h>
#include <unistd.h>
#include <stdlib.h>
#include <string.h>

#include "phxrpc_addrbook_tool.h"
#include "addrbook_tool_impl.h"

#include "addrbook_client.h"

#include "phxrpc/file.h"

using namespace phxrpc;

void showUsage( const char * program )
{
    printf( "\nUsage: %s [-c <config>] [-f <func>] [-v]\n", program );

    AddrbookTool::Name2Func_t * name2func = AddrbookTool::GetName2Func();

    for( int i = 0; ; i++ ) {
        AddrbookTool::Name2Func_t * iter = &( name2func[i] );

        if( NULL == iter->name ) break;

        printf( "    -f %s %s\n", iter->name, iter->usage );
    }
    printf( "\n" );
    exit( 0 );
}

int main( int argc, char * argv[] )
{
    const char * func = NULL;
    const char * config = NULL;

    for( int i = 1; i < argc - 1; i++ ) {
        if( 0 == strcmp( argv[i], "-c" ) ) {
            config = argv[ ++i ];
        }
        if( 0 == strcmp( argv[i], "-f" ) ) {
            func = argv[ ++i ];
        }
        if( 0 == strcmp( argv[i], "-v" ) ) {
            showUsage( argv[0] );
        }
    }

    if( NULL == func ) showUsage( argv[0] );

    if( NULL != config ) AddrbookClient::Init( config );

    AddrbookTool::Name2Func_t * target = NULL;

    AddrbookTool::Name2Func_t * name2func = AddrbookTool::GetName2Func();

    for( int i = 0; i < 100; i++ ) {
        AddrbookTool::Name2Func_t * iter = &( name2func[i] );

        if( NULL == iter->name ) break;

        if( 0 == strcasecmp( func, iter->name ) ) {
            target = iter;
            break;
        }
    }

    if( NULL == target ) showUsage( argv[0] );

    OptMap opt_map( target->opt_string );

    if( ! opt_map.Parse( argc, argv ) ) showUsage( argv[0] );

    AddrbookTool::ToolFunc_t targefunc = target->func;

    AddrbookToolImpl tool;

    if( 0 != ( tool.*targefunc ) ( opt_map ) ) showUsage( argv[0] );

    return 0;
}
